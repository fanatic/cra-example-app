name: CI/CD

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    uses: fanatic/actions/.github/workflows/create-react-app.yml@main
    with:
      username: mona
